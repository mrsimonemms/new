# Monorepo

Sets up a monorepo designed for NodeJS and Golang projects but can be extended.

```shell
make monorepo
```

This supports components in Golang and NodeJS, because that's primarily what I
use. Additional languages can be added fairly trivially.
